# Lab6实验报告

## 思考题

### Thinking 6.1

> Thinking6.1示例代码中，父进程操作管道的写端，子进程操作管道的读端。如果现在想 让父进程作为“读者”，代码应当如何修改？

* 代码如下

  ```c
  #include <stdlib.h>
  #include <unistd.h>
  
  int fildes[2];
  char buf[100];
  int status;
  
  int main(){
  
      status = pipe(fildes);
      
      if (status == -1 ) {
      	printf("error\n");
      }
  
  
      switch (fork()) {
      	case -1:
      		break;
      	case 0: /* 子进程 - 作为管道的写者 */
      		close(fildes[0]); /* 关闭不用的读端 */
      		write(fildes[1], "Hello world\n", 12); /* 向管道中写数据 */
      		close(fildes[1]); /* 写入结束，关闭写端 */
      		exit(EXIT_SUCCESS);
      
      	default: /* 父进程 - 作为管道的读者 */
              close(fildes[1]); /* 关闭不用的写端 */
      		read(fildes[0], buf, 100); /* 从管道中读数据 */
      		printf("father-process read:%s",buf); /* 打印读到的数据 */
      		close(fildes[0]); /* 读取结束，关闭读端 */
      		exit(EXIT_SUCCESS);
      }
  }
  ```


### Thinking 6.2

>  Thinking 6.2 上面这种不同步修改 pp_ref 而导致的进程竞争问题在 user/lib/fd.c 中 的dup 函数中也存在。请结合代码模仿上述情景，分析一下我们的 dup函数中为什么会出 现预想之外的情况？

*  `dup`的功能是将旧的文件描述符及其内容复制到新的文件描述符上去。先映射文件描述符所在页，再映射其所对应的文件页面。如果时间片结束刚好处于两步之间，此时去到另一个进程，会出现认为映射完成但实际上文件未完全映射完成的情况，从而导致错误。

### Thinking 6.3 

> Thinking 6.3 阅读上述材料并思考：为什么系统调用一定是原子操作呢？如果你觉得不是 所有的系统调用都是原子操作，请给出反例。希望能结合相关代码进行分析说明。

* 查看`kern/entry.S`的中断处理代码可知：

  ```assembly
  exc_gen_entry:
  	SAVE_ALL
  	/*
  	* Note: When EXL is set or UM is unset, the processor is in kernel mode.
  	* When EXL is set, the value of EPC is not updated when a new exception occurs.
  	* To keep the processor in kernel mode and enable exception reentrancy,
  	* we unset UM and EXL, and unset IE to globally disable interrupts.
  	*/
  	mfc0    t0, CP0_STATUS
  	and     t0, t0, ~(STATUS_UM | STATUS_EXL | STATUS_IE)
  	mtc0    t0, CP0_STATUS
  	mfc0    t0, CP0_CAUSE
  	andi    t0, 0x7c
  	lw      t0, exception_handlers(t0)
  	jr      t0
  ```

  当进入系统调用时，本质上就是进行了中断陷入内核态，此时会执行`mfc0    t0, CP0_STATUS`，即对中断位进行写入，如果写入为0，即不可中断，则为原子操作，否则则不是原子操作。

### Thinking 6.4

> Thinking 6.4 仔细阅读上面这段话，并思考下列问题 
>
> * 按照上述说法控制 pipe_close中 fd和 pipeunmap的顺序，是否可以解决上述场 景的进程竞争问题？给出你的分析过程。 
> * 我们只分析了 close时的情形，在 fd.c中有一个 dup函数，用于复制文件描述符。 试想，如果要复制的文件描述符指向一个管道，那么是否会出现与 close类似的问 题？请模仿上述材料写写你的理解。

* 可以。未完成状态下`pipe`的引用大于`fd`的引用，先对`fd`解除映射不会导致`pipe`引用次数与`fd`引用次数相等。
* 会。通过调换映射文件描述符和映射文件页面的顺序可以解决这个问题。

### Thinking 6.5

> Thinking 6.5 思考以下三个问题。 
>
> * 认真回看Lab5文件系统相关代码，弄清打开文件的过程。 
> *  回顾Lab1与Lab3，思考如何读取并加载ELF文件。 
> *  在Lab1 中我们介绍了 data text bss 段及它们的含义，data 段存放初始化过的全 局变量，bss段存放未初始化的全局变量。关于memsize和filesize，我们在Note 1.3.4中也解释了它们的含义与特点。关于Note1.3.4，注意其中关于“bss段并不在文 件中占数据”表述的含义。回顾Lab3并思考：elf_load_seg()和load_icode_mapper() 函数是如何确保加载ELF文件时，bss段数据被正确加载进虚拟内存空间。bss段 在ELF中并不占空间，但ELF加载进内存后，bss段的数据占据了空间，并且初始 值都是0。请回顾elf_load_seg() 和 load_icode_mapper() 的实现，思考这一点 是如何实现的？ 

* elf_load_seg()中：

  ```c
  for (i = offset ? MIN(bin_size, PAGE_SIZE - offset) : 0; i < bin_size; i += PAGE_SIZE) {
  		if ((r = map_page(data, va + i, 0, perm, bin + i, MIN(bin_size - i, PAGE_SIZE))) !=
  		    0) {
  			return r;
  ```

  加载内容可能小于分配空间（即  `filesize` 和`memsize` ），在加载二进制镜像的时候为差值部分预留出来空间填充为0。这一部分就是.bss段的相关空间。这样.bss段就被正确加载进了虚拟内存空间。

### Thinking 6.6

> Thinking 6.6 通过阅读代码空白段的注释我们知道，将标准输入或输出定向到文件，需要 我们将其dup到0或1号文件描述符（fd）。那么问题来了：在哪步，0和1被“安排”为 标准输入和标准输出？请分析代码执行流程，给出答案。

* 在`user/init.c`中`main()`有

  ```c
  // stdin should be 0, because no file descriptors are open yet
  if ((r = opencons()) != 0) {
      user_panic("opencons: %d", r);
  }
  // stdout
  if ((r = dup(0, 1)) < 0) {
      user_panic("dup: %d", r);
  }
  ```

  `opencons()`分配一个新的文件描述符，配置设备类型为控制台。由于前没有分配过别的文件描述符，因此分配的结果是标准输入（0号）。

  之后将0号描述符的内容全部映射到1号描述符，即标准输出。

### Thinking 6.7

> Thinking 6.7 在 shell 中执行的命令分为内置命令和外部命令。在执行内置命令时shell不 需要fork 一个子 shell，如 Linux 系统中的 cd 命令。在执行外部命令时 shell 需要 fork 一个子shell，然后子 shell 去执行这条命令。 据此判断，在MOS 中我们用到的 shell 命令是内置命令还是外部命令？请思考为什么 Linux 的 cd 命令是内部命令而不是外部命令？

* 外部命令。在MOS中，每一次执行命令都要调用`runcmd()`。`runcmd()`调用`spawn()`函数生成一个子shell。
* `cd` 所做的是改变 shell 的 PWD。 如果 cd 是一个外部命令，那么它改变的将会是子 shell 的 PWD，而不是shell的PWD。

###  Thinking 6.8 

>  Thinking 6.8 在你的 shell 中输入命令 ls.b | cat.b > motd。 
>
> * 请问你可以在你的shell 中观察到几次spawn？分别对应哪个进程？ 
> *  请问你可以在你的shell 中观察到几次进程销毁？分别对应哪个进程？

- shell中进行了2次`spawn`，这两次生成的子进程分别用于执行`ls.b`和`cat.b`
- shell中进行了2次进程销毁，这2个进程分别是shell执行`ls.b`和`cat.b`时通过`spawn`生成的子进程。

## 难点与体会

* 本次实验的内容量和工作量相比以往的lab来说比较少，但还是有难点，关键就在于有关`spawn`函数的作用理解：
  1. **文件读取与验证**：
     - 根据输入文件路径打开文件。
     - 使用`readn`函数读取程序节头表信息。
     - 利用`elf_from`检查文件有效性。
  2. **子进程创建**：
     - 通过`syscall_exofork`系统调用创建子进程。
     - 在子进程中准备加载程序内容。
  3. **栈空间设置**：
     - 使用`init_stack`为子进程设置栈空间。
     - 栈空间结构具体如图所示。
  4. **程序加载**：
     - 分段读入程序，检查是否为需加载段。
     - 使用`elf_load_seg`和`spawn_mapper`加载数据到合适位置。
     - `spawn_mapper`与`load_icode_mapper`相似，但多使用系统调用以适应用户态。
  5. **子进程上下文设置**：
     - 重新设置程序开始运行位置。
     - 设置栈指针。
  6. **共享页面设置**：
     - 配置父子进程间共享的页面。
  7. **子进程运行**：
     - 允许子进程运行，完成spawn过程。
* 作为操作系统课程的最后一次lab，感触良多。本学期的os学习不能说非常成功，只能说勉强进行，几乎所有课下内容都需要按照课程组给出的答案才能正常完成，所幸限时测试中的exam部分指导很清晰，因此完成的较为顺利，**extra则几乎没有什么成就。总的来说，还是学习到了许多，感谢课程组和助教的努力，也对自己的失败进行反思。**